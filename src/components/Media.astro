---
import type { DetailedMovie, DetailedTv } from '@/types/index'
import Header from '@/components/media/Header.astro'

interface Props {
  mediaData: DetailedMovie & DetailedTv
}

const { mediaData } = Astro.props
---

<div>
  <Header mediaData={mediaData} />
</div>
