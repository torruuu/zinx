---
import Layout from '@/layouts/Layout.astro'
import MainBanner from '@/components/home/MainBanner.vue'
import { getLangFromUrl } from '@/i18n/utils'

const lang = getLangFromUrl(Astro.url)
---

<Layout>
  <MainBanner lang={lang} client:load />
</Layout>
