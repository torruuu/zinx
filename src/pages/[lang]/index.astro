---
import Layout from '@/layouts/Layout.astro'
import Home from '@/components/Home.vue'
import { getLangFromUrl } from '@/i18n/utils'

const lang = getLangFromUrl(Astro.url)
---

<Layout>
  <Home lang={lang} client:load />
</Layout>
